/// A Company's budget. Immutable domain model only — no services, no
/// calculations. Its internal shape is intentionally undefined at this
/// foundational stage.
class Budget {
  const Budget();
}
