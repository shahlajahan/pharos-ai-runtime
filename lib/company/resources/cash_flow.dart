/// A Company's cash flow. Immutable domain model only — no services, no
/// calculations. Its internal shape is intentionally undefined at this
/// foundational stage.
class CashFlow {
  const CashFlow();
}
