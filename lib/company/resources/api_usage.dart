/// A Company's API usage. Immutable domain model only — no services, no
/// calculations. Its internal shape is intentionally undefined at this
/// foundational stage.
class ApiUsage {
  const ApiUsage();
}
