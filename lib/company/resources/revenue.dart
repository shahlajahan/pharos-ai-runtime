/// A Company's revenue. Immutable domain model only — no services, no
/// calculations. Its internal shape is intentionally undefined at this
/// foundational stage.
class Revenue {
  const Revenue();
}
