/// A Company's expenses. Immutable domain model only — no services, no
/// calculations. Its internal shape is intentionally undefined at this
/// foundational stage.
class Expenses {
  const Expenses();
}
